# PlayerOps 模块 UX 评审与整改计划

> 评审对象：35 条 UX 挑刺报告 + 实测 bug「好友弹飞不可用」
> 结论：**认同 25 条、部分认同 7 条、不认同 3 条**；按"改动成本 × 体验收益"归入 A/B/C 三批。

---

## 0. 实测 Bug：弹飞不可用（已修复）

| 项 | 说明 |
| --- | --- |
| 根因 | ① 玩家的 Rigidbody **不在 Player 根对象**上，而在 `PlayerMovement._rig`（反编译确认），`GetComponent<Rigidbody>()` 拿到 null → 静默失效；② 即使拿到，**客户端改 velocity 是非法写**，服务器权威会回滚 |
| 修复 | 主机门控 + 反射取 `MovementRig` + `AddForce(Impulse)` 走物理管线（比直接设 linearVelocity 更难被移动逻辑覆盖） |
| 状态 | 已编译（0 错误/5 警告），payload 已同步 |

---

## 0.5 实施状态（A+B+C 全做）

| 批次 | 项 | 状态 |
| --- | --- | --- |
| A | F2 活跃指示器 / F3 确认优化 / S3 显示选中 / S5 任务进度 / S6 操作反馈 / S9 动态刷新 / S10 效果标签 / M1 循环预设 / M2 上限 99 / M4 名字截断 / L1 自救区 / L2 改名 / L3 真实时间 / L6 超时 3s | ✅ 全部完成 |
| B | F1 Player 引用索引 / F4 非主机横幅 / M5 开关置灰 / S4 危险按钮红色 / M11 任务暂停删除 | ✅ 完成 |
| B | S1+S2 单页平铺重构 | 🟡 现状已是"页签→4 区块平铺"无子菜单（F9→队友页→选人→操作 = 4 步）；左侧常驻列表属更大布局重构，暂缓 |
| C | M9 撤销击杀 / S7 ←→ 键盘切人 / S11 ⚠ 实验标注 / M8 自动关菜单 / L5 ESP 统一入口 / M3 文案 / S12 日志高亮 | ✅ 完成 |
| C | F5 内嵌选物器 / M10 搜索框 / L4 踢人原因 / M6 背包满提示 | ⏳ backlog（依赖生成器/背包 API 深改） |

---

## 1. 逐条评审

图例：✅ 认同 / 🟡 部分认同（有取舍）/ ❌ 不认同（保持现状）

### 致命级

| # | 评审 | 方案 | 批次 |
| --- | --- | --- | --- |
| F1 索引用 int 掉线杀错人 | ✅ 最该改 | 状态集合的 key 从 `int` 改为 `Player` 引用，每次操作前校验 `(bool)p && PlayerManager.Players.Contains(p)`，失效自动取消选中。**改动面**：PlayerOps 内全部 HashSet/Dictionary 的 key 类型 + PlayerMonitor/CheatPanel 选中态 | B |
| F2 持续效果无全局指示 | ✅ | 状态栏加活跃效果计数 `🔒锁定×N 🧲吸人×N ⏱任务×M`（只读状态，零网络成本），点击弹出"全部解除" | A |
| F3 二次确认模式差 | 🟡 | "点一下变确认？"模式在修掉嵌套 bug 后是**正常工作**的，但确实无取消、超时无提示。改进：超时 2s→3s，点**其它任何按钮自动清除**当前确认态（天然取消），按钮旁显示剩余秒数 | A |
| F4 非主机全灰无解释 | ✅ | 页签顶部加横幅「⚠ 以下操作需主机权限」，Custom 区块内危险按钮统一 `GUI.enabled = CheatOps.IsServerReady` 置灰 | B |
| F5 给物资跨页操作 | 🟡 | 单体给物复用生成浏览器选中项（已做）+ 批量页补"未选物品提示"（已做）。内嵌物品选择器属大改，列 backlog | C |

### 严重级

| # | 评审 | 方案 | 批次 |
| --- | --- | --- | --- |
| S1 下拉慢不支持多选 | ✅ | 左侧常驻列表 + 右侧操作区（与 S2 合并为一次布局重构） | B |
| S2 层级过深 | 🟡 | 当前"队友→4 子菜单"已是两级；单页平铺是布局重构。折中：子菜单入口行**显示当前选中玩家名**（`玩家列表 [玩家A] ›`），常用操作少一级跳转 | B |
| S3 批量页不显示选中 | ✅ | 批量页顶部常驻 `当前选中：玩家A`；未选中时"仅选中"选项置灰 | A |
| S4 按钮无视觉层级 | ✅ | 危险操作（杀/击飞/抛投）红色文字 + 分隔线；毁灭性（杀全队/清背包/封禁）加深红底 + 独立分组 | B |
| S5 循环任务无进度 | ✅ | 运行中按钮显示 `执行中 12/50 [停止]`（TaskScheduler 已有 RemainingCycles） | A |
| S6 无即时反馈 | ✅ | 复用 ConfirmButton 的临时状态机制，操作后按钮短暂显示 `✓ 已执行` / `✗ 失败`（1.5s） | A |
| S7 键盘导航失效 | 🟡 | Custom 区块键盘跳过是**既有设计决策**（CheatPanel 注释明确）。可加 ←→ 切换玩家，但优先级低 | C |
| S8 页签顺序打乱 | ❌ | 新增功能必然改变页签结构，"放最后"同样打乱肌肉记忆；且"队友"页签位置是用户上轮选的。保持现状 | — |
| S9 信息 0.5s 刷新过时 | ✅ | PlayerMonitor 支持动态间隔：有选中玩家时 0.2s，否则 0.5s | A |
| S10 效果叠加不可见 | ✅ | 信息卡加效果标签 `🔒锁定 🚫禁购 📦卸装 ☠中毒 🔥燃烧`，PlayerInfo 字段已备好，纯 UI 展示 | A |
| S11 实验性无标注 | ✅ | `弹球/倒吊/缩放/变物种` 标 `⚠实验`；恢复入口已存在（解除所有限制） | C |
| S12 日志筛选 | 🟡 | 已有分类过滤 Toolbar；加"重要操作高亮背景" | C |

### 中等级

| # | 评审 | 方案 | 批次 |
| --- | --- | --- | --- |
| M1 循环快捷预设 | ✅ | 循环次数输入框旁加 `[1][5][10][50]` | A |
| M2 无上限警告 | 🟡 | 已有 clamp 999（不是无限）；降为 99 并超 10 提示"次数过多可能卡顿" | A |
| M3 无 tooltip | 🟡 | IMGUI tooltip 需 hover 基建；先用按钮文案自解释（"吸队友"→"吸选中玩家"） | C |
| M4 名字溢出 | ✅ | 下拉/信息卡名字截断 12 字符 + "…" | A |
| M5 非主机开关静默无效 | ✅ | 限制类开关 `GUI.enabled = IsServerReady` 置灰（与 F4 同批） | B |
| M6 背包满无提示 | 🟡 | 依赖背包容量 API，需反编译确认；列 backlog | C |
| M7 传送分散 3 处 | 🟡 | 玩家页内已集中大部分；房主页"玩家传送"行保留作兼容 | C |
| M8 菜单挡画面 | ✅ | 系统页加"执行操作后自动关菜单"开关（默认关，尊重原行为） | A |
| M9 撤销 | 🟡 | 击杀→复活可撤，其它难；先做"最近一次操作可撤销（限击杀/清背包）" | C |
| M10 列表搜索 | ✅ | 16 人房间场景，玩家列表上方加搜索框过滤 | C |
| M11 任务管理器 | ✅ | TaskScheduler 已可枚举任务；加暂停/编辑/单独删除 | B |

### 轻微级

| # | 评审 | 方案 | 批次 |
| --- | --- | --- | --- |
| L1 复活自己位置别扭 | ✅ | 移到"系统"页或列表页顶部"自救"分组 | A |
| L2 "仅非主机"语义模糊 | ✅ | 改名"除房主外全队" | A |
| L3 日志用游戏内时间 | 🟡 | 用 unscaledTime 便于对照游戏事件；改 `DateTime.Now` 更符合截图场景。改成真实时间 | A |
| L4 踢人原因 | ✅ | 踢人前弹输入框（复用已有文本框机制） | C |
| L5 ESP 开关分离 | 🟡 | 生物 ESP 在"玩家"页视觉子菜单，玩家 ESP 在"队友"页；统一入口需动菜单树，低优先 | C |
| L6 高 DPI 确认超时 | ✅ | 随 F3 处理（3s + 自动取消） | A |
| L7 音效 | 🟡 | IMGUI 音效成本高收益低；危险操作改用文字/颜色强反馈代替 | C |

### 不认同明细（3 条）

1. **S8 页签顺序**：位置是用户上轮亲自选的，"放最后"不解决问题，还会再乱一次。
2. **F3 完全否定点两下**：模态框需要全新 UI 基建（IMGUI 无原生 modal），长按与鼠标操作冲突；"点两下 + 点其它按钮取消 + 剩余秒数"是成本最低且足够安全的方案。
3. **L3 坚持真实时间**（部分不认同）：游戏内时间在复盘时更有用，但既然要截图发人，改真实时间是对的——归入 A 批执行。

---

## 2. 整改批次

### 批次 A「小而高价值，1 小时内可完成」（12 项）
F2 活跃效果指示器 · F3 确认优化 · S3 批量页显示选中 · S5 任务进度 · S6 操作反馈 · S9 动态刷新 · S10 效果标签 · M1 循环预设 · M2 上限 99 · M4 名字截断 · L1 自救分组 · L2 改名 · L3 真实时间 · L6 超时

### 批次 B「架构级，需整体规划」（6 项）
F1 Player 引用索引 · F4 非主机引导 · S1+S2 单页平铺布局 · S4 按钮视觉层级 · M5 开关置灰 · M11 任务管理器

### 批次 C「体验打磨 backlog」（16 项）
F5 内嵌选物器 · S7 键盘 · S11 实验标注 · S12 日志高亮 · M3/M6/M7/M8/M9/M10 · L4/L5/L7

---

## 3. 结论

- **报告整体质量高**：35 条中 25 条直接成立，3 条有异议（S8/F3/L3），其余是成本取舍。
- **核心矛盾判断准确**：模块确实是"功能清单驱动"而非"操作流驱动"。批次 B 的布局重构（左侧玩家列表+右侧操作）是解决 S1/S2/S3/S10 的钥匙。
- **弹飞 bug 已修**：根因是拿错 Rigidbody + 客户端非法写，与 UX 报告无关但属同类"服务器权威"问题。
